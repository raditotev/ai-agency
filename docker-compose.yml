services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agency-website
    ports:
      - '${PORT:-3000}:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
