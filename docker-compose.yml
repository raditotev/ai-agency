services:
  web:
    image: ai-agency:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agency
    ports:
      - '${PORT:-3000}:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
